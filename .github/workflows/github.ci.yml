name: CI/CD

on:
  pull_request:
    branches: ["main"]
  push:
    branches: ["main"]

concurrency:
  group: cicd-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  build-and-check:
    name: Build & Check
    runs-on: ubuntu-latest

    env:
      DATABASE_URL: "file:./test.db"

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Generate Prisma Client
        run: npx prisma generate

      - name: Lint
        run: npm run lint

      - name: Typecheck
        run: npm run typecheck

      - name: Run Tests
        run: npm test

      - name: Build
        run: npm run build

  deploy-production:
    name: Deploy to Vercel (Production)
    needs: build-and-check
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install Vercel CLI
        run: npm i -g vercel

      # Hämtar Vercel-projektets inställningar + env-konfig så builden matchar Vercel
      - name: Pull Vercel environment (production)
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_AUTH_TOKEN }}
          VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
          VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}
        run: vercel pull --yes --environment=production --token=$VERCEL_TOKEN

      # Bygger i CI med Vercel’s builder (detta blir artefakten som deployas)
      - name: Build with Vercel (production)
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_AUTH_TOKEN }}
        run: vercel build --prod --token=$VERCEL_TOKEN

      # Deployar exakt det som byggdes ovan (ingen extra build på Vercel)
      - name: Deploy prebuilt (Production)
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_AUTH_TOKEN }}
        run: vercel deploy --prebuilt --prod --token=$VERCEL_TOKEN
